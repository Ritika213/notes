var is globally scoped while let & const are block scoped.
var can be updated & re-declared within its scope.
let can be updale but not re-declared.
const can neither be updated nor be re-declared.
var variable are initialized with undefiend where as let and const variable are not initialized.
const must be initialized during declation unlike let and var.
